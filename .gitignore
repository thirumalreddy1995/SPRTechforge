# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (commit .env.example only)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Break-glass: Firebase Admin SDK keys (see RUNBOOK.md §1.2). Never commit.
scripts/.firebase-admin-key.json
scripts/*.serviceAccount*.json

# Backups (see RUNBOOK.md §1.1). Keep out of git; store on encrypted local disk.
backups/
